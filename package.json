{
  "name": "color-ranker-crud-express-mongo",
  "version": "1.0.0",
  "description": "A server that handles the backend for the color ranker site",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Robert M. Nollan",
  "license": "Beerware",
  "dependencies": {
    "express": "^4.18.1"
  }
}
